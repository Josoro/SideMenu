import 'package:flutter/material.dart';

class DescrptionBook extends StatelessWidget {
  const DescrptionBook({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
